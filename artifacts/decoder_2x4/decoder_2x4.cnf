Let inputs be A,B and outputs Y0..Y3 with
Y0 = ¬A⋀¬B, Y1 = ¬A⋀B, Y2 = A⋀¬B, Y3 = A⋀B.
In CNF (using ! for ¬):

Y0 = (!Y0 ∨ !A) ∧ (!Y0 ∨ !B) ∧ (A ∨ B ∨ Y0)

Y1 = (!Y1 ∨ !A) ∧ (!Y1 ∨ B)  ∧ (A ∨ !B ∨ Y1)

Y2 = (!Y2 ∨ A)  ∧ (!Y2 ∨ !B) ∧ (!A ∨ B ∨ Y2)

Y3 = (!Y3 ∨ A)  ∧ (!Y3 ∨ B)  ∧ (!A ∨ !B ∨ Y3)